# Frontend environment variables (copy to .env.local for local dev)
# API base url for the backend (include /api). Example for local dev:
VITE_API_URL=https://milkman-backend-rweo.onrender.com/api
# When deploying on Vercel, set VITE_API_URL to your backend URL (e.g., https://your-backend.onrender.com/api)
